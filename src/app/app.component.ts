import { Component } from '@angular/core';

@Component({
    selector: 'mw-app', //DOM Element selector for rendering
    templateUrl: './app.component.html',
    styleUrls: ['./app.component.css'] //Array of style sheet URLs to be applied to the HTML provided through either the template or templateUrl metadata
}) 
export class AppComponent {}