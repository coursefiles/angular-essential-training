import { Component } from '@angular/core';

@Component({
    selector: 'mw-app', //DOM Element selector for rendering
    template: `
        <h1>Media Watch App</h1>
        <p>Keeping track of the media I want to watch.</p>
    ` //HTML Template to be rendered
})
export class AppComponent {}